# Agents are installed by setup.sh — not tracked in this repo
# agents/

# Exception: custom agents defined in this repo
!agents/agents-orchestrator.md

# opencode plugin deps (managed by opencode itself)
node_modules/
package.json
bun.lock

# DCP plugin config (generated by plugin installer)
dcp.jsonc

# Per-project dirs (should not exist here, but just in case)
.opencode/

# Cursor config (created by agency-agents install)
.cursor/
token-dashboard.html
token-baselines/
